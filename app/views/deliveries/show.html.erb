
<div class="head_deliveries">
  <h1><%= @delivery.name %></h1>
  <div class="nav-item dropdown">
    <%= image_tag "https://kitt.lewagon.com/placeholder/users/ssaunier", class: "avatar dropdown-toggle", id: "navbarDropdown", data: { bs_toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
    <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
      <%= link_to "My deliveries", my_deliveries_path, class: "dropdown-item" %>
      <%# if current_user.function == "manager" %>
      <%= link_to "Add a Loading", new_delivery_loading_path(@delivery), class: "dropdown-item" %>
      <%# end %>
    </div>
  </div>
</div>



  <div class="container py-4">
    <div class="cards mb-3">
        <% Loading.where(delivery_id: @delivery).each do |loading| %>
          <%= link_to delivery_loading_path(@delivery, loading) do %>
            <% if loading.done? %>
              <div class="card-delivery" style="border-color:#a5e0a5;border-style:solid">
                <div class="card-delivery-infos">
                  <h2><%= loading.name %></h2>
                  <p> <%= loading.address.split(', ').last %></p>
                  <p> <%= loading.date %></p>
                </div>
              </div>
            <% else  %>
              <div class="card-delivery" style="border-color:#dd8787;border-style:solid">
                <div class="card-delivery-infos">
                  <h2><%= loading.name %></h2>
                  <p> <%= loading.address.split(', ').last %></p>
                  <p> <%= loading.date %></p>
                </div>
              </div>
            <% end %>
          <% end %>
        <% end %>
      </div>
    </div>
  <ul id="progress">
  <% Loading.where(delivery_id: @delivery).each_with_index do |loading, index| %>
      <% if index == 0 %>
        <% if loading.done? %>
          <div class="progress-step">
            <div class="progress-schema">
              <li><div class="node green"></div></li>
            </div>
            <div class="progress-info"><h3><%= loading.name %></h3></div>
          </div>
          <% else %>
          <div class="progress-step">
            <li><div class="node grey"></div></li>
            <div class="progress-info"><h3><%= loading.name %></h3></div>
          </div>
          <% end %>
      <% else %>
        <% if loading.done? %>
          <div class="progress-step">
              <div class="progress-schema">
              </div>
              <div class="progress-schema">
                <li><div class="divider green"></div></li>
                <li><div class="node green">
                    <div class="progress-info"><h3><%= loading.name %></h3></div>

                </div></li>
              </div>
          </div>


        <% else %>
          <li><div class="divider grey"></div></li>
          <li><div class="node grey"></div><p><%= loading.name %></p></li>
        <% end %>
      <% end %>
<<<<<<< HEAD
  <% end %>
  </ul>
<%# ------------------- %>
  <ul id="progress">
  <% Loading.where(delivery_id: @delivery).each_with_index do |loading, index| %>
      <% if index == 0 %>
        <% if loading.done? %>


              <li>
                <div class="node green"></div>
                  <div class="progress-info">
                    <p><%= loading.name %></p>
                    <p> <%= loading.address.split(', ').last %></p>
                    <p> <%= loading.date %></p>
                  </div>
              </li>



          <% else %>
            <li><div class="node grey"></div><p><%= loading.name %></p></li>

          <% end %>
      <% else %>
        <% if loading.done? %>




                <li><div class="divider green"></div></li>
                <li><div class="node green"></div><p><%= loading.name %></p></li>





        <% else %>
          <li><div class="divider grey"></div></li>
          <li><div class="node grey"></div><p><%= loading.name %></p></li>
        <% end %>
      <% end %>
  <% end %>
  </ul>



=======
    </div>
  </div>


<div id="map"
    style="width:100%"
    data-markers="<%= @markers.to_json %>"
    data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
</div>

<%= link_to "Add a Loading", new_delivery_loading_path(@delivery), class: "btn btn-primary bottom mx-auto d-block"  %>
>>>>>>> master

  <% if Loading.where(delivery_id: @delivery).presence %>
    <div id="map"
        style="width:100%"
        data-markers="<%= @markers.to_json %>"
        data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
    </div>
  <% end %>

</div>
